---
title: Typography fixture
description: Exercises every element the prose stylesheet targets.
date: 2026-08-23
tags: ['testing']
authors: ['vikram']
---

import Callout from '@/components/callout.astro'

Leading paragraph with **strong text**, a [link](https://example.com), some
`inline code`, and a <kbd>Ctrl</kbd>+<kbd>K</kbd> chord.

## Heading two

Paragraph under h2. Another sentence to force wrapping across a couple of lines
so leading is visible.

### Heading three

#### Heading four

##### Heading five

###### Heading six

- Unordered item one
- Unordered item two
  - Nested item A
  - Nested item B
- Unordered item three

1. Ordered item one
2. Ordered item two
   1. Nested ordered A
   2. Nested ordered B

> A blockquote with a paragraph.
>
> - and a list
> - inside it
>
> plus `code` in the quote.

| Column A | Column B | Right |
| -------- | -------- | ----: |
| one      | two      |     3 |
| four     | five     |     6 |

---

```ts
const greet = (name: string): string => {
  return `hello ${name}`
}
```

<details>
  <summary>A disclosure summary</summary>

Body of the details element, which gets the compact treatment.

- with a list
- of two items

</details>

<Callout variant="note">A note callout.</Callout>
<Callout variant="warning">A warning callout.</Callout>

Math: $E = mc^2$ inline, and display:

$$
\int_0^\infty e^{-x^2}\,dx = \frac{\sqrt{\pi}}{2}
$$

Final paragraph.
